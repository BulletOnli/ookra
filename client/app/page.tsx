"use client";
import { Tooltip } from "@chakra-ui/react";

const Homepage = () => {
    return (
        <div>
            <Tooltip hasArrow label="hello">
                hi
            </Tooltip>
        </div>
    );
};

export default Homepage;
